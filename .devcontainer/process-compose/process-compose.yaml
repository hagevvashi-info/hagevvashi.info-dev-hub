version: "0.5"

# TUIの設定
log_location: /tmp/process-compose-${USER}.log
log_level: info

processes:
  # difit（開発支援ツール）
  difit:
    command: "difit"
    working_dir: "/home/hagevvashi/hagevvashi.info-dev-hub"
    availability:
      restart: "no"  # エラーを見たいので自動再起動しない
    environment:
      - HOME=/home/hagevvashi

  # 実験的なサービスの例
  # vite-preview:
  #   command: "npm run preview"
  #   working_dir: "/home/hagevvashi/repos/some-project"
  #   availability:
  #     restart: "no"
  #   depends_on:
  #     difit:
  #       condition: process_started

  # アプリケーションサーバーの例
  # app-server:
  #   command: "npm run dev"
  #   working_dir: "/home/hagevvashi/repos/product-a"
  #   availability:
  #     restart: "no"
  #   environment:
  #     - HOME=/home/hagevvashi
  #     - PORT=8080